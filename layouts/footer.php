 <!-- Contact Section -->
 <section id="contact" class="py-20 bg-gray-100 dark:bg-dark">
     <div class="container mx-auto px-4 sm:px-6 lg:px-8">
         <h2 class="text-3xl md:text-4xl font-bold mb-12 text-center">
             <span class="gradient-text">Get In Touch</span>
         </h2>

         <div class="flex flex-col lg:flex-row gap-8 max-w-6xl mx-auto">
             <!-- Contact Info -->
             <div class="lg:w-1/2">
                 <div
                     class="bg-white dark:bg-darker p-8 rounded-xl border border-gray-200 dark:border-gray-700 shadow-lg h-full flex flex-col">
                     <h3 class="text-2xl font-semibold mb-6 text-primary-light dark:text-primary-dark">Let's Collaborate
                     </h3>
                     <p class="text-gray-600 dark:text-gray-400 mb-8 leading-relaxed flex-grow">
                         I'm currently available for freelance projects or full-time opportunities. Whether you have
                         a specific project in mind, need technical consultation, or just want to connect, feel free
                         to reach out.
                     </p>

                     <div class="space-y-6">
                         <!-- Email -->
                         <div class="flex items-start">
                             <div class="bg-primary-light/10 dark:bg-primary-dark/10 p-3 rounded-lg mr-4 flex-shrink-0">
                                 <i class="fas fa-envelope text-primary-light dark:text-primary-dark text-xl"></i>
                             </div>
                             <div>
                                 <h4 class="font-medium text-lg mb-1">Email</h4>
                                 <a href="mailto:shivam300m@gmail.com"
                                     class="text-gray-600 dark:text-gray-400 hover:text-primary-light dark:hover:text-primary-dark transition-colors">
                                     shivam300m@gmail.com
                                 </a>
                             </div>
                         </div>

                         <!-- Phone -->
                         <div class="flex items-start">
                             <div class="bg-primary-light/10 dark:bg-primary-dark/10 p-3 rounded-lg mr-4 flex-shrink-0">
                                 <i class="fas fa-phone-alt text-primary-light dark:text-primary-dark text-xl"></i>
                             </div>
                             <div>
                                 <h4 class="font-medium text-lg mb-1">Phone</h4>
                                 <a href="tel:+917522074049"
                                     class="text-gray-600 dark:text-gray-400 hover:text-primary-light dark:hover:text-primary-dark transition-colors">
                                     +91 7522074049
                                 </a>
                             </div>
                         </div>

                         <!-- Location -->
                         <div class="flex items-start">
                             <div class="bg-primary-light/10 dark:bg-primary-dark/10 p-3 rounded-lg mr-4 flex-shrink-0">
                                 <i class="fas fa-map-marker-alt text-primary-light dark:text-primary-dark text-xl"></i>
                             </div>
                             <div>
                                 <h4 class="font-medium text-lg mb-1">Location</h4>
                                 <p class="text-gray-600 dark:text-gray-400">Lucknow, Uttar Pradesh (226012)</p>
                             </div>
                         </div>
                     </div>

                     <!-- Social Links -->
                     <div class="mt-10 pt-6 border-t border-gray-200 dark:border-gray-700">
                         <h4 class="font-medium text-lg mb-4">Connect With Me</h4>
                         <div class="flex space-x-4">
                             <a href="https://github.com/yourusername" target="_blank"
                                 class="w-12 h-12 bg-gray-100 dark:bg-gray-800 rounded-full flex items-center justify-center border border-gray-200 dark:border-gray-700 hover:border-primary-light dark:hover:border-primary-dark hover:text-primary-light dark:hover:text-primary-dark transition-all transform hover:scale-110">
                                 <i class="fab fa-github text-xl"></i>
                             </a>
                             <a href="https://linkedin.com/in/yourusername" target="_blank"
                                 class="w-12 h-12 bg-gray-100 dark:bg-gray-800 rounded-full flex items-center justify-center border border-gray-200 dark:border-gray-700 hover:border-primary-light dark:hover:border-primary-dark hover:text-primary-light dark:hover:text-primary-dark transition-all transform hover:scale-110">
                                 <i class="fab fa-linkedin-in text-xl"></i>
                             </a>
                             <a href="https://twitter.com/yourusername" target="_blank"
                                 class="w-12 h-12 bg-gray-100 dark:bg-gray-800 rounded-full flex items-center justify-center border border-gray-200 dark:border-gray-700 hover:border-primary-light dark:hover:border-primary-dark hover:text-primary-light dark:hover:text-primary-dark transition-all transform hover:scale-110">
                                 <i class="fab fa-twitter text-xl"></i>
                             </a>
                             <a href="https://instagram.com/yourusername" target="_blank"
                                 class="w-12 h-12 bg-gray-100 dark:bg-gray-800 rounded-full flex items-center justify-center border border-gray-200 dark:border-gray-700 hover:border-primary-light dark:hover:border-primary-dark hover:text-primary-light dark:hover:text-primary-dark transition-all transform hover:scale-110">
                                 <i class="fab fa-instagram text-xl"></i>
                             </a>
                         </div>
                     </div>
                 </div>
             </div>

             <!-- Contact Form -->
             <div class="lg:w-1/2">
                 <div
                     class="bg-white dark:bg-darker p-8 rounded-xl border border-gray-200 dark:border-gray-700 shadow-lg h-full">
                     <h3 class="text-2xl font-semibold mb-6 text-primary-light dark:text-primary-dark">Send Me a Message
                     </h3>
                     <form id="contactForm" class="space-y-6" action="javascript:void(0)">
                         <!-- Name Field -->
                         <div>
                             <label for="name"
                                 class="block mb-2 text-sm font-medium text-gray-600 dark:text-gray-400">Full
                                 Name</label>
                             <div class="relative">
                                 <div class="absolute inset-y-0 left-0 pl-3 flex items-center pointer-events-none">
                                     <i class="fas fa-user text-gray-500 dark:text-gray-400"></i>
                                 </div>
                                 <input type="text" id="name" name="name"
                                     class="pl-10 w-full px-4 py-3 bg-gray-50 dark:bg-gray-800 border border-gray-200 dark:border-gray-700 rounded-lg focus:outline-none focus:ring-2 focus:ring-primary-light/50 dark:focus:ring-primary-dark/50 focus:border-transparent placeholder-gray-400 dark:placeholder-gray-500 text-gray-700 dark:text-gray-300 transition-all"
                                     placeholder="Enter your name" required>
                             </div>
                         </div>

                         <!-- Email Field -->
                         <div>
                             <label for="email"
                                 class="block mb-2 text-sm font-medium text-gray-600 dark:text-gray-400">Email
                                 Address</label>
                             <div class="relative">
                                 <div class="absolute inset-y-0 left-0 pl-3 flex items-center pointer-events-none">
                                     <i class="fas fa-envelope text-gray-500 dark:text-gray-400"></i>
                                 </div>
                                 <input type="email" id="email" name="email"
                                     class="pl-10 w-full px-4 py-3 bg-gray-50 dark:bg-gray-800 border border-gray-200 dark:border-gray-700 rounded-lg focus:outline-none focus:ring-2 focus:ring-primary-light/50 dark:focus:ring-primary-dark/50 focus:border-transparent placeholder-gray-400 dark:placeholder-gray-500 text-gray-700 dark:text-gray-300 transition-all"
                                     placeholder="your@email.com" required>
                             </div>
                         </div>

                         <!-- Subject Field -->
                         <div>
                             <label for="subject"
                                 class="block mb-2 text-sm font-medium text-gray-600 dark:text-gray-400">Subject</label>
                             <div class="relative">
                                 <div class="absolute inset-y-0 left-0 pl-3 flex items-center pointer-events-none">
                                     <i class="fas fa-tag text-gray-500 dark:text-gray-400"></i>
                                 </div>
                                 <input type="text" id="subject" name="subject"
                                     class="pl-10 w-full px-4 py-3 bg-gray-50 dark:bg-gray-800 border border-gray-200 dark:border-gray-700 rounded-lg focus:outline-none focus:ring-2 focus:ring-primary-light/50 dark:focus:ring-primary-dark/50 focus:border-transparent placeholder-gray-400 dark:placeholder-gray-500 text-gray-700 dark:text-gray-300 transition-all"
                                     placeholder="What's this about?" required>
                             </div>
                         </div>

                         <!-- Message Field -->
                         <div>
                             <label for="message"
                                 class="block mb-2 text-sm font-medium text-gray-600 dark:text-gray-400">Your
                                 Message</label>
                             <div class="relative">
                                 <div class="absolute top-3 left-3">
                                     <i class="fas fa-comment text-gray-500 dark:text-gray-400"></i>
                                 </div>
                                 <textarea id="message" rows="4" name="message"
                                     class="pl-10 w-full px-4 py-3 bg-gray-50 dark:bg-gray-800 border border-gray-200 dark:border-gray-700 rounded-lg focus:outline-none focus:ring-2 focus:ring-primary-light/50 dark:focus:ring-primary-dark/50 focus:border-transparent placeholder-gray-400 dark:placeholder-gray-500 text-gray-700 dark:text-gray-300 transition-all"
                                     placeholder="Hello Shivam, I'd like to talk about..." required></textarea>
                             </div>
                         </div>

                         <!-- Submit Button -->
                         <div class="pt-2">
                             <button type="submit"
                                 class="w-full px-6 py-3.5 bg-gradient-to-r from-primary-light dark:from-primary-dark to-accent-light dark:to-accent-dark rounded-lg font-medium text-white hover:opacity-90 transition-all shadow-lg shadow-primary-light/30 dark:shadow-primary-dark/30 hover:shadow-primary-light/40 dark:hover:shadow-primary-dark/40 transform hover:-translate-y-0.5">
                                 Send Message
                                 <i class="fas fa-paper-plane ml-2"></i>
                             </button>
                         </div>
                     </form>
                 </div>
             </div>
         </div>
 </section>
 <!-- Footer -->
 <footer class="bg-gray-900 text-white py-12">
     <div class="container mx-auto px-4 sm:px-6 lg:px-8">
         <div class="text-center">
             <h3 class="text-2xl font-bold gradient-text mb-4">Shivam Mishra</h3>
             <p class="text-gray-400 mb-6">Full Stack Developer | Creating Digital Excellence</p>

             <div class="flex justify-center gap-6 mb-8">
                 <a href="#" class="text-gray-400 hover:text-white transition-colors">
                     <i class="fab fa-github text-xl"></i>
                 </a>
                 <a href="#" class="text-gray-400 hover:text-white transition-colors">
                     <i class="fab fa-linkedin text-xl"></i>
                 </a>
                 <a href="#" class="text-gray-400 hover:text-white transition-colors">
                     <i class="fab fa-twitter text-xl"></i>
                 </a>
                 <a href="#" class="text-gray-400 hover:text-white transition-colors">
                     <i class="fab fa-instagram text-xl"></i>
                 </a>
             </div>

             <div class="border-t border-gray-800 pt-8">
                 <p class="text-gray-400 text-sm">
                     © 2025 Shivam Mishra. All rights reserved. | Built with ❤️ using modern web technologies
                 </p>
             </div>
         </div>
     </div>
     <div class="smart-scroller">
         <button class="scroller-btn" aria-label="Scroll" title="Scroll">
             <svg class="scroll-icon" viewBox="0 0 24 24" width="20" height="20">
                 <path class="up-arrow" d="M12 4l-8 8h5v8h6v-8h5z" fill="currentColor" />
                 <path class="down-arrow" d="M12 20l8-8h-5V4h-6v8H4z" fill="currentColor" />
             </svg>
         </button>
     </div>

 </footer>
 <!-- SweetAlert2 CSS -->
 <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/sweetalert2@11/dist/sweetalert2.min.css">

 <!-- SweetAlert2 JS -->
 <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>

 <script>
     document.addEventListener('DOMContentLoaded', function() {
         const scrollerBtn = document.querySelector('.scroller-btn');
         const scrollThreshold = 300;
         const bottomThreshold = 100;

         function updateScroller() {
             const scrollPosition = window.pageYOffset;
             const windowHeight = window.innerHeight;
             const documentHeight = document.body.scrollHeight;
             // Hide if at top or bottom
             if (scrollPosition < scrollThreshold &&
                 scrollPosition + windowHeight >= documentHeight - bottomThreshold) {
                 scrollerBtn.classList.remove('visible');
                 return;
             }
             // Determine direction
             if (scrollPosition + windowHeight >= documentHeight - bottomThreshold) {
                 scrollerBtn.classList.add('scroll-up');
                 scrollerBtn.classList.remove('scroll-down');
                 scrollerBtn.setAttribute('aria-label', 'Scroll to top');
                 scrollerBtn.setAttribute('title', 'Go to top');
             } else {
                 scrollerBtn.classList.add('scroll-down');
                 scrollerBtn.classList.remove('scroll-up');
                 scrollerBtn.setAttribute('aria-label', 'Scroll to bottom');
                 scrollerBtn.setAttribute('title', 'Go to bottom');
             }
             scrollerBtn.classList.add('visible');
         }

         function handleScroll() {
             window.scrollTo({
                 top: scrollerBtn.classList.contains('scroll-up') ? 0 : document.body.scrollHeight,
                 behavior: 'smooth'
             });
         }
         // Initial check
         updateScroller();
         // Listen for scroll events
         window.addEventListener('scroll', updateScroller);
         // Button click handler
         scrollerBtn.addEventListener('click', function(e) {
             e.preventDefault();
             handleScroll();
         });
     });
 </script>

 <script>
     /**
      * Portfolio Website JavaScript - Complete Implementation
      * Author: Shivam Mishra
      * Features: Theme Toggle, Mobile Menu, Smooth Scrolling, Skills Tabs, Contact Form, Animations
      */
     (function() {
         'use strict';
         // ======================
         // INITIALIZATION
         // ======================
         document.addEventListener('DOMContentLoaded', function() {
             initializePortfolio();
         });

         function initializePortfolio() {
             // Initialize all components
             ThemeManager.init();
             MobileMenu.init();
             Navigation.init();
             SkillsTabs.init();
             ContactForm.init();
             ScrollAnimations.init();
             Utils.init();
             console.log('Portfolio initialized successfully!');
         }
         // ======================
         // THEME MANAGER
         // ======================
         const ThemeManager = {
             init() {
                 this.themeToggleDesktop = document.getElementById('theme-toggle');
                 this.themeToggleMobile = document.getElementById('theme-toggle-mobile');
                 this.setupTheme();
                 this.bindEvents();
             },
             setupTheme() {
                 // Get saved theme or system preference
                 const savedTheme = localStorage.getItem('theme');
                 const systemPrefersDark = window.matchMedia('(prefers-color-scheme: dark)').matches;
                 const currentTheme = savedTheme || (systemPrefersDark ? 'dark' : 'light');
                 this.applyTheme(currentTheme);
             },
             applyTheme(theme) {
                 const html = document.documentElement;
                 const body = document.body;
                 if (theme === 'dark') {
                     html.classList.add('dark');
                     body.classList.add('dark');
                     html.style.colorScheme = 'dark';
                 } else {
                     html.classList.remove('dark');
                     body.classList.remove('dark');
                     html.style.colorScheme = 'light';
                 }
                 // Force reflow
                 html.offsetHeight;
                 this.updateThemeIcons(theme);
             },
             updateThemeIcons(theme) {
                 const moonIcons = document.querySelectorAll('.fa-moon');
                 const sunIcons = document.querySelectorAll('.fa-sun');
                 moonIcons.forEach(icon => {
                     icon.style.display = theme === 'dark' ? 'none' : 'inline-block';
                 });
                 sunIcons.forEach(icon => {
                     icon.style.display = theme === 'dark' ? 'inline-block' : 'none';
                 });
             },
             toggleTheme() {
                 const isDark = document.documentElement.classList.contains('dark');
                 const newTheme = isDark ? 'light' : 'dark';
                 // Add transition
                 document.documentElement.style.transition = 'all 0.3s ease';
                 this.applyTheme(newTheme);
                 localStorage.setItem('theme', newTheme);
                 // Remove transition after animation
                 setTimeout(() => {
                     document.documentElement.style.transition = '';
                 }, 300);
             },
             bindEvents() {
                 // Desktop theme toggle
                 if (this.themeToggleDesktop) {
                     this.themeToggleDesktop.addEventListener('click', () => this.toggleTheme());
                 }
                 // Mobile theme toggle
                 if (this.themeToggleMobile) {
                     this.themeToggleMobile.addEventListener('click', () => this.toggleTheme());
                 }
                 // Listen for system theme changes
                 window.matchMedia('(prefers-color-scheme: dark)').addEventListener('change', (e) => {
                     if (!localStorage.getItem('theme')) {
                         this.applyTheme(e.matches ? 'dark' : 'light');
                     }
                 });
             }
         };
         // ======================
         // MOBILE MENU
         // ======================
         const MobileMenu = {
             init() {
                 this.menuButton = document.getElementById('mobile-menu-button');
                 this.mobileMenu = document.getElementById('mobile-menu');
                 this.isOpen = false;
                 if (this.menuButton && this.mobileMenu) {
                     this.bindEvents();
                     this.setupStyles(); // Ensure proper z-index and positioning
                 }
             },
             setupStyles() {
                 // Dynamically set mobile menu below header height
                 const header = document.querySelector('header');
                 if (header) {
                     const headerHeight = header.offsetHeight;
                     this.mobileMenu.style.top = `${headerHeight}px`;
                 }
                 // Ensure menu stays above other content
                 this.mobileMenu.style.zIndex = '1000';
             },
             bindEvents() {
                 // Toggle menu on button click
                 this.menuButton.addEventListener('click', (e) => {
                     e.preventDefault();
                     e.stopPropagation(); // Prevent immediate document click handler
                     this.toggle();
                 });
                 // Close when clicking nav links
                 this.mobileMenu.querySelectorAll('a').forEach(link => {
                     link.addEventListener('click', () => this.close());
                 });
                 // Close when clicking outside
                 document.addEventListener('click', (e) => {
                     if (this.isOpen && !this.mobileMenu.contains(e.target)) {
                         this.close();
                     }
                 });
                 // Close on Escape key
                 document.addEventListener('keydown', (e) => {
                     if (this.isOpen && e.key === 'Escape') this.close();
                 });
                 // Close when resizing to desktop
                 window.addEventListener('resize', () => {
                     if (window.innerWidth >= 768 && this.isOpen) {
                         this.close();
                     }
                 });
             },
             toggle() {
                 this.isOpen ? this.close() : this.open();
             },
             open() {
                 this.mobileMenu.classList.remove('hidden');
                 this.isOpen = true;
                 this.updateIcon('close');
                 document.body.style.overflow = 'hidden'; // Prevent page scrolling
             },
             close() {
                 this.mobileMenu.classList.add('hidden');
                 this.isOpen = false;
                 this.updateIcon('menu');
                 document.body.style.overflow = ''; // Re-enable scrolling
             },
             updateIcon(type) {
                 const icons = {
                     menu: `<svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16"/>
            </svg>`,
                     close: `<svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12"/>
            </svg>`
                 };
                 this.menuButton.innerHTML = icons[type];
             }
         };
         // ======================
         // NAVIGATION
         // ======================
         const Navigation = {
             init() {
                 this.sections = document.querySelectorAll('section[id]');
                 this.navLinks = document.querySelectorAll('.nav-link');
                 this.header = document.querySelector('header');
                 this.bindEvents();
             },
             bindEvents() {
                 // Smooth scrolling
                 this.setupSmoothScrolling();
                 // Active link tracking
                 this.setupActiveLinks();
                 // Header scroll effect
                 this.setupHeaderEffect();
             },
             setupSmoothScrolling() {
                 const links = document.querySelectorAll('a[href^="#"]');
                 links.forEach(link => {
                     link.addEventListener('click', (e) => {
                         const href = link.getAttribute('href');
                         if (href === '#') {
                             e.preventDefault();
                             return;
                         }
                         const target = document.querySelector(href);
                         if (target) {
                             e.preventDefault();
                             const headerHeight = this.header ? this.header.offsetHeight : 0;
                             const targetPosition = target.offsetTop - headerHeight - 20;
                             window.scrollTo({
                                 top: targetPosition,
                                 behavior: 'smooth'
                             });
                         }
                     });
                 });
             },
             setupActiveLinks() {
                 let scrollTimeout;
                 const updateActiveLink = () => {
                     const scrollPosition = window.pageYOffset + 150;
                     let currentSection = '';
                     this.sections.forEach(section => {
                         const sectionTop = section.offsetTop;
                         const sectionHeight = section.clientHeight;
                         if (scrollPosition >= sectionTop &&
                             scrollPosition < sectionTop + sectionHeight) {
                             currentSection = section.getAttribute('id');
                         }
                     });
                     // Update nav links
                     this.navLinks.forEach(link => {
                         link.classList.remove('active');
                         if (link.getAttribute('href') === `#${currentSection}`) {
                             link.classList.add('active');
                         }
                     });
                 };
                 window.addEventListener('scroll', () => {
                     if (scrollTimeout) {
                         cancelAnimationFrame(scrollTimeout);
                     }
                     scrollTimeout = requestAnimationFrame(updateActiveLink);
                 });
                 // Initial update
                 updateActiveLink();
             },
             setupHeaderEffect() {
                 if (!this.header) return;
                 let scrollTimeout;
                 window.addEventListener('scroll', () => {
                     if (scrollTimeout) {
                         cancelAnimationFrame(scrollTimeout);
                     }
                     scrollTimeout = requestAnimationFrame(() => {
                         const scrolled = window.scrollY > 50;
                         if (scrolled) {
                             this.header.style.backdropFilter = 'blur(10px)';
                             this.header.style.boxShadow = '0 4px 6px -1px rgba(0, 0, 0, 0.1)';
                         } else {
                             this.header.style.backdropFilter = 'blur(4px)';
                             this.header.style.boxShadow = '0 1px 3px 0 rgba(0, 0, 0, 0.1)';
                         }
                     });
                 });
             }
         };
         // ======================
         // SKILLS TABS
         // ======================
         const SkillsTabs = {
             init() {
                 this.tabButtons = document.querySelectorAll('.tab-btn');
                 this.tabPanes = document.querySelectorAll('.tab-pane');
                 this.bindEvents();
                 this.setInitialTab();
             },
             bindEvents() {
                 this.tabButtons.forEach(button => {
                     button.addEventListener('click', (e) => {
                         e.preventDefault();
                         const targetTab = button.getAttribute('data-tab');
                         this.switchTab(targetTab);
                     });
                 });
             },
             switchTab(targetTabId) {
                 // Remove active states
                 this.tabButtons.forEach(btn => btn.classList.remove('active-tab'));
                 this.tabPanes.forEach(pane => {
                     pane.classList.add('hidden');
                     pane.classList.remove('active');
                 });
                 // Set active states
                 const targetPane = document.getElementById(targetTabId);
                 const activeButton = document.querySelector(`[data-tab="${targetTabId}"]`);
                 if (targetPane) {
                     targetPane.classList.remove('hidden');
                     targetPane.classList.add('active');
                 }
                 if (activeButton) {
                     activeButton.classList.add('active-tab');
                 }
             },
             setInitialTab() {
                 if (this.tabButtons.length > 0) {
                     const firstTabId = this.tabButtons[0].getAttribute('data-tab') || 'all';
                     this.switchTab(firstTabId);
                 }
             }
         };
         const ContactForm = {
             init() {
                 this.form = document.querySelector('#contact form');
                 if (this.form) {
                     this.bindEvents();
                 }
             },
             bindEvents() {
                 this.form.addEventListener('submit', (e) => this.handleSubmit(e));
                 const fields = this.form.querySelectorAll('input, textarea');
                 fields.forEach(field => {
                     field.addEventListener('input', () => this.clearFieldError(field));
                     field.addEventListener('blur', () => this.validateField(field));
                 });
             },
             async handleSubmit(e) {
                 e.preventDefault();
                 const formData = this.getFormData();
                 if (!this.validateForm(formData)) {
                     return;
                 }
                 await this.submitForm(formData);
             },
             getFormData() {
                 return {
                     name: this.getFieldValue('name'),
                     email: this.getFieldValue('email'),
                     subject: this.getFieldValue('subject'),
                     message: this.getFieldValue('message')
                 };
             },
             getFieldValue(fieldId) {
                 const field = document.getElementById(fieldId);
                 return field ? field.value.trim() : '';
             },
             validateForm(data) {
                 let isValid = true;
                 if (!data.name) {
                     this.showFieldError('name', 'Name is required');
                     isValid = false;
                 }
                 if (!data.email) {
                     this.showFieldError('email', 'Email is required');
                     isValid = false;
                 } else if (!this.isValidEmail(data.email)) {
                     this.showFieldError('email', 'Please enter a valid email address');
                     isValid = false;
                 }
                 if (!data.subject) {
                     this.showFieldError('subject', 'Subject is required');
                     isValid = false;
                 }
                 if (!data.message) {
                     this.showFieldError('message', 'Message is required');
                     isValid = false;
                 } else if (data.message.length < 10) {
                     this.showFieldError('message', 'Message must be at least 10 characters long');
                     isValid = false;
                 }
                 return isValid;
             },
             validateField(field) {
                 const value = field.value.trim();
                 const fieldId = field.id;
                 switch (fieldId) {
                     case 'name':
                         if (!value) {
                             this.showFieldError(fieldId, 'Name is required');
                             return false;
                         }
                         break;
                     case 'email':
                         if (!value) {
                             this.showFieldError(fieldId, 'Email is required');
                             return false;
                         } else if (!this.isValidEmail(value)) {
                             this.showFieldError(fieldId, 'Please enter a valid email address');
                             return false;
                         }
                         break;
                     case 'subject':
                         if (!value) {
                             this.showFieldError(fieldId, 'Subject is required');
                             return false;
                         }
                         break;
                     case 'message':
                         if (!value) {
                             this.showFieldError(fieldId, 'Message is required');
                             return false;
                         } else if (value.length < 10) {
                             this.showFieldError(fieldId, 'Message must be at least 10 characters long');
                             return false;
                         }
                         break;
                 }
                 this.clearFieldError(field);
                 return true;
             },
             async submitForm(data) {
                 const submitButton = this.form.querySelector('button[type="submit"]');
                 const originalText = submitButton.dataset.originalText || submitButton.innerHTML;
                 submitButton.dataset.originalText = originalText;
                 submitButton.disabled = true;
                 submitButton.innerHTML =
                     '<i class="fas fa-spinner fa-spin mr-2"></i><span class="sending-text">Sending...</span>';
                 try {
                     const response = await fetch('contact_us.php', {
                         method: 'POST',
                         headers: {
                             'Content-Type': 'application/json',
                             'Accept': 'application/json'
                         },
                         body: JSON.stringify(data)
                     });
                     if (!response.ok) {
                         throw new Error(`HTTP error! status: ${response.status}`);
                     }
                     const result = await response.json();
                     if (result.success) {
                         this.showNotification('Thank you! Your message has been sent successfully.',
                             'success');
                         this.form.reset();
                         this.clearAllErrors();
                     } else {
                         this.showNotification(result.message ||
                             'There was an error sending your message. Please try again.', 'error');
                     }
                 } catch (error) {
                     console.error('Form submission error:', error);
                     this.showNotification(
                         'Sorry, there was a network error. Please check your connection and try again.',
                         'error');
                 } finally {
                     submitButton.innerHTML = submitButton.dataset.originalText;
                     submitButton.disabled = false;
                 }
             },
             showFieldError(fieldId, message) {
                 const field = document.getElementById(fieldId);
                 if (!field) return;
                 this.clearFieldError(field);
                 const errorDiv = document.createElement('div');
                 errorDiv.className = 'field-error text-red-500 text-sm mt-1';
                 errorDiv.textContent = message;
                 field.parentNode.appendChild(errorDiv);
                 field.classList.add('border-red-500', 'focus:border-red-500');
                 field.style.animation = 'shake 0.3s ease-in-out';
                 setTimeout(() => {
                     field.style.animation = '';
                 }, 300);
             },
             clearFieldError(field) {
                 if (!field) return;
                 const errorDiv = field.parentNode.querySelector('.field-error');
                 if (errorDiv) {
                     errorDiv.remove();
                 }
                 field.classList.remove('border-red-500', 'focus:border-red-500');
             },
             clearAllErrors() {
                 const errorDivs = document.querySelectorAll('.field-error');
                 errorDivs.forEach(error => error.remove());
                 const errorFields = document.querySelectorAll('.border-red-500');
                 errorFields.forEach(field => {
                     field.classList.remove('border-red-500', 'focus:border-red-500');
                 });
             },
             showNotification(message, type) {
                 const notification = document.createElement('div');
                 notification.className = `notification fixed top-4 right-4 p-4 rounded-lg shadow-lg z-50 max-w-sm ${
            type === 'success' ? 'bg-green-500 text-white' : 'bg-red-500 text-white'
        }`;
                 notification.innerHTML = `
            <div class="flex items-center">
                <i class="fas ${type === 'success' ? 'fa-check-circle' : 'fa-exclamation-circle'} mr-2"></i>
                <span>${message}</span>
                <button class="ml-auto text-white hover:text-gray-200" onclick="this.parentElement.parentElement.remove()">
                    <i class="fas fa-times"></i>
                </button>
            </div>
        `;
                 document.body.appendChild(notification);
                 setTimeout(() => {
                     if (notification.parentElement) {
                         notification.remove();
                     }
                 }, 5000);
             },
             isValidEmail(email) {
                 const emailRegex = /^[^\s@]+@[^\s@]+\.[^\s@]+$/;
                 return emailRegex.test(email);
             }
         };
         document.addEventListener('DOMContentLoaded', () => {
             ContactForm.init();
         });
         // ======================
         // SCROLL ANIMATIONS
         // ======================
         const ScrollAnimations = {
             init() {
                 this.setupIntersectionObserver();
                 this.animateSkillBars();
             },
             setupIntersectionObserver() {
                 const options = {
                     threshold: 0.1,
                     rootMargin: '0px 0px -50px 0px'
                 };
                 const observer = new IntersectionObserver((entries) => {
                     entries.forEach(entry => {
                         if (entry.isIntersecting) {
                             this.animateElement(entry.target);
                         }
                     });
                 }, options);
                 // Observe elements
                 const elements = document.querySelectorAll('.card-hover, .equal-height');
                 elements.forEach(el => {
                     el.style.opacity = '0';
                     el.style.transform = 'translateY(30px)';
                     el.style.transition = 'all 0.6s ease-out';
                     observer.observe(el);
                 });
             },
             animateElement(element) {
                 element.style.opacity = '1';
                 element.style.transform = 'translateY(0)';
             },
             animateSkillBars() {
                 const skillBars = document.querySelectorAll('.skill-bar');
                 const observer = new IntersectionObserver((entries) => {
                     entries.forEach(entry => {
                         if (entry.isIntersecting) {
                             const percentage = entry.target.dataset.percentage || '90';
                             entry.target.style.width = percentage + '%';
                         }
                     });
                 });
                 skillBars.forEach(bar => observer.observe(bar));
             }
         };
         // ======================
         // UTILITIES
         // ======================
         const Utils = {
             init() {
                 this.setupKeyboardNavigation();
                 this.setupPerformanceOptimizations();
             },
             setupKeyboardNavigation() {
                 // Escape key to close mobile menu
                 document.addEventListener('keydown', (e) => {
                     if (e.key === 'Escape' && MobileMenu.isOpen) {
                         MobileMenu.close();
                     }
                 });
             },
             setupPerformanceOptimizations() {
                 // Debounce resize events
                 let resizeTimeout;
                 window.addEventListener('resize', () => {
                     clearTimeout(resizeTimeout);
                     resizeTimeout = setTimeout(() => {
                         this.handleResize();
                     }, 150);
                 });
             },
             handleResize() {
                 // Close mobile menu on desktop
                 if (window.innerWidth >= 768 && MobileMenu.isOpen) {
                     MobileMenu.close();
                 }
             },
             showNotification(message, type = 'info') {
                 // Remove existing notifications
                 const existing = document.querySelectorAll('.notification-toast');
                 existing.forEach(notification => notification.remove());
                 const notification = document.createElement('div');
                 notification.className =
                     'notification-toast fixed top-4 right-4 px-6 py-4 rounded-lg shadow-lg z-50 max-w-sm transform translate-x-full';
                 const colors = {
                     success: 'bg-green-500 text-white',
                     error: 'bg-red-500 text-white',
                     info: 'bg-blue-500 text-white',
                     warning: 'bg-yellow-500 text-white'
                 };
                 notification.className += ' ' + colors[type];
                 notification.innerHTML = `
                <div class="flex items-center justify-between">
                    <div class="flex items-center">
                        <i class="fas ${this.getNotificationIcon(type)} mr-3"></i>
                        <span>${message}</span>
                    </div>
                    <button class="ml-4 text-white hover:text-gray-200 focus:outline-none" onclick="this.parentElement.parentElement.remove()">
                        <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12"/>
                        </svg>
                    </button>
                </div>
            `;
                 document.body.appendChild(notification);
                 // Animate in
                 requestAnimationFrame(() => {
                     notification.style.transform = 'translateX(0)';
                     notification.style.transition = 'transform 0.3s ease-out';
                 });
                 // Auto remove
                 setTimeout(() => {
                     if (notification.parentNode) {
                         notification.style.transform = 'translateX(100%)';
                         setTimeout(() => notification.remove(), 300);
                     }
                 }, 5000);
             },
             getNotificationIcon(type) {
                 const icons = {
                     success: 'fa-check-circle',
                     error: 'fa-exclamation-circle',
                     info: 'fa-info-circle',
                     warning: 'fa-exclamation-triangle'
                 };
                 return icons[type] || icons.info;
             },
             throttle(func, limit) {
                 let inThrottle;
                 return function() {
                     const args = arguments;
                     const context = this;
                     if (!inThrottle) {
                         func.apply(context, args);
                         inThrottle = true;
                         setTimeout(() => inThrottle = false, limit);
                     }
                 };
             },
             debounce(func, delay) {
                 let timeoutId;
                 return function(...args) {
                     clearTimeout(timeoutId);
                     timeoutId = setTimeout(() => func.apply(this, args), delay);
                 };
             }
         };
         // ======================
         // CSS INJECTION FOR ANIMATIONS
         // ======================
         const additionalCSS = `
        <style>
            /* Enhanced animations and transitions */
            .notification-toast {
                backdrop-filter: blur(10px);
                border: 1px solid rgba(255, 255, 255, 0.1);
            }
            
            @keyframes shake {
                0%, 100% { transform: translateX(0); }
                10%, 30%, 50%, 70%, 90% { transform: translateX(-5px); }
                20%, 40%, 60%, 80% { transform: translateX(5px); }
            }
            
            @keyframes fadeInUp {
                from {
                    opacity: 0;
                    transform: translateY(30px);
                }
                to {
                    opacity: 1;
                    transform: translateY(0);
                }
            }
            
            .tab-pane.active {
                animation: fadeInUp 0.5s ease-out;
            }
            
            .nav-link.active::after {
                width: 100% !important;
            }
            
            .tab-btn.active-tab {
                border-bottom-color: #8b5cf6 !important;
                color: #8b5cf6 !important;
            }
            
            .dark .tab-btn.active-tab {
                border-bottom-color: #7c3aed !important;
                color: #7c3aed !important;
            }
            
            /* Dark mode enhancements */
            .dark {
                color-scheme: dark;
            }
            
            .dark body {
                background-color: #020617;
                color: #d1d5db;
            }
            
            .dark .bg-white {
                background-color: #1f2937;
            }
            
            .dark .bg-gray-100 {
                background-color: #111827;
            }
            
            .dark .text-gray-700 {
                color: #d1d5db;
            }
            
            .dark .text-gray-600 {
                color: #9ca3af;
            }
            
            .dark .border-gray-200 {
                border-color: #374151;
            }
            
            .dark .border-gray-300 {
                border-color: #4b5563;
            }
            
            .dark input,
            .dark textarea {
                background-color: #374151;
                border-color: #4b5563;
                color: #f9fafb;
            }
            
            .dark input::placeholder,
            .dark textarea::placeholder {
                color: #9ca3af;
            }
            
            /* Smooth transitions */
            * {
                transition: background-color 0.3s ease, color 0.3s ease, border-color 0.3s ease;
            }
            
            /* Performance optimizations */
            .card-hover {
                will-change: transform;
            }
            
            .nav-link::after {
                will-change: width;
            }
            
            /* Focus styles for accessibility */
            .focus-visible:focus {
                outline: 2px solid #6366f1;
                outline-offset: 2px;
            }
            
            /* Loading states */
            .loading {
                pointer-events: none;
                opacity: 0.7;
            }
            
            .spinner {
                animation: spin 1s linear infinite;
            }
            
            @keyframes spin {
                to { transform: rotate(360deg); }
            }
        </style>
    `;
         // Inject CSS
         document.head.insertAdjacentHTML('beforeend', additionalCSS);
     })();
 </script>